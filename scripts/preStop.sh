#!/bin/bash
# Unused hook — the actual stop logic lives in postStop.sh, matching the
# convention this plugin's lifecycle scripts are modeled on (Remote Falcon's
# FPP plugin). Kept as an explicit no-op rather than omitted, so it's clear
# this was a deliberate choice, not an oversight.

#preStop
